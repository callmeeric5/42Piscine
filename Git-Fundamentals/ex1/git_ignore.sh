git ls-files -o -i --exclude-standard

# -o / --othters show untracked files
# -i / --ignored show only ignored files, must be used with either -c or -o
# --exclude-standard add standard git exclusion
